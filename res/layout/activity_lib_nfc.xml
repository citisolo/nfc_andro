<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".LibNfcActivity" >

    <TextView
        android:id="@+id/vwLog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:background="#0000FF"
        android:height="450dp"
        android:hint="#FFFFFF"
        android:textColor="#FFFFFF"
        android:maxLines="1000"
        android:scrollbars="vertical"
        android:text="Press button to launch diagnostics..."
        android:width="420dp" />

    <Button
        android:id="@+id/btnStart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/vwLog"
        android:layout_alignParentBottom="true"
        android:layout_below="@+id/vwLog"
        android:onClick="start_server"
        android:text="Start server" />

    <Button
        android:id="@+id/btnTestUsb"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/btnStart"
        android:layout_below="@+id/vwLog"
        android:layout_centerHorizontal="true"
        android:onClick="test_usb"
        android:text="test usb" />

</RelativeLayout>
